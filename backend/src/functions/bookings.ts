import { APIGatewayProxyHandler } from 'aws-lambda';

import { createBooking } from '../core/bookings';

export const createBookingHandler: APIGatewayProxyHandler = async (event) => {
  // implementation will come later
};
